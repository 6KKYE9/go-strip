# go-strip

一堆文本要处理，开编辑器点来点去太慢，命令行一把梭最省事。

去掉每行首尾空白，零依赖，从管道读、往管道写。

## 用法

```bash
cat file.txt | go run .        # 只去首尾空白
cat file.txt | go run . -b     # 再去空行
```

典型用途是清洗从网页/Excel 拷出来的文本。读标准输入，所以直接接管道最顺手。
